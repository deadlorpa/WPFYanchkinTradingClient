<UserControl x:Class="WPFYanchkinTradingClient.DealsTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400">
    <DataGrid
            ItemsSource="{Binding Deals, Mode=TwoWay}">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Продавец" Binding="{Binding DealerName}" Width="90" />
            <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="80" />
            <DataGridTextColumn Header="Регион" Binding="{Binding Region}" Width="50" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
